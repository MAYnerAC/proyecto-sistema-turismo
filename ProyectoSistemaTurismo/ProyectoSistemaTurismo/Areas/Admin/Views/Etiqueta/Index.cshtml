@model IEnumerable<ProyectoSistemaTurismo.Models.Etiqueta>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nombre_etiqueta)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.estado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nombre_etiqueta)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.estado)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_etiqueta }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_etiqueta }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_etiqueta })
        </td>
    </tr>
}

</table>
